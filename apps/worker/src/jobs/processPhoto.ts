/**
 * Job `process_photo` — implementação 007.
 * Processa fotos com sharp: EXIF orientation, JPEG, lado maior ≤ 2500 px,
 * qualidade 82, thumbnail 400 px. Concorrência 4 (RNF-04).
 */
export async function processPhoto(): Promise<void> {
  throw new Error(
    'process_photo ainda não implementado — ver implementação 007.',
  );
}
