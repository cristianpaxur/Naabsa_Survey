/**
 * Job `retention_purge` — implementação 010.
 * Diário: relatórios `generated` há mais de 30 dias têm fotos e planilha
 * apagadas do Storage (mantendo PDFs e dados); status → `purged`.
 */
export async function retentionPurge(): Promise<void> {
  throw new Error(
    'retention_purge ainda não implementado — ver implementação 010.',
  );
}
