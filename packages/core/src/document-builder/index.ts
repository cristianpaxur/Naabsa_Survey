// Document-builder (template → JSON TipTap) — implementação 004 (T-001..T-003).
// Aqui viverão: builders por tipo, construtores de nós (photoFrame, dataTable,
// mark dataField) e o conteúdo estático por tipo×variante (content/).
export {};
