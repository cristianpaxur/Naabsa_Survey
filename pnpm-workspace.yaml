packages:
  - 'apps/*'
  - 'packages/*'

# Dependências nativas autorizadas a rodar scripts de build (pnpm 11+).
# sharp: processamento de imagem do worker (impl 007).
# esbuild: motor do tsx (runtime TypeScript do worker).
onlyBuiltDependencies:
  - sharp
  - esbuild
